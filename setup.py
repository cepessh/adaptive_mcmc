from setuptools import setup, find_packages

setup(
    name="adaptive_mcmc",
    version="0.1",
    packages=find_packages(),   # finds adaptive_mcmc and sub‑packages
    install_requires=[],       # put runtime deps here, e.g. ["torch>=1.12"]
)
